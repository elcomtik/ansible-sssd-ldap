---
- name: packages | install packages
  become: true
  package:
    name: "{{ item }}"
    state: present
  with_items: "{{ sssd_ldap_packages }}"

- name: packages | ensure nscd is absent
  become: true
  package:
    name: nscd
    state: absent
